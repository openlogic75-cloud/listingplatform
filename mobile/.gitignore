# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
/build/

# IDE
.idea/
*.iml
*.ipr
*.iws

# Platform folders (android/, ios/) are generated once by
# `flutter create . --org com.listingplatform --project-name listingplatform`
# and then committed on a machine with the Flutter SDK installed.
